# ─────────────────────────────────────────────────────────────
# Operação Mundo — variáveis de ambiente (RAIZ / docker compose)
# Copie para `.env` e ajuste conforme necessário. NUNCA versione o `.env`.
# ─────────────────────────────────────────────────────────────

# --- PostgreSQL (consumido pelo docker-compose.yml) ---
POSTGRES_USER=mundo
POSTGRES_PASSWORD=mundo_dev
POSTGRES_DB=operacao_mundo
POSTGRES_PORT=5433
